{
  "_note": "25 Hz policy = 2 physics steps of 20 ms per action; goals stay inside the +-1 rad action range and well inside the +-1.7 rad joint limits. PPO settings assume the direct transport with --n-envs 16: n_steps is per env (rollout = 16 x 50 = 800), 400k steps take about 5 minutes at ~1500 env steps/s. History: 1 env over ROS needed n_steps 400 / 40k steps (37 min) with log_std_init -1.5 to reach 0.026 rad.",
  "task": {
    "physics_steps_per_action": 2,
    "episode_steps": 50,
    "goal_range": [
      -0.8,
      0.8
    ],
    "command_settle_s": 0.005,
    "reward": {
      "tracking_l1": -1.0,
      "action_rate": -0.05
    }
  },
  "train": {
    "algo": "ppo",
    "total_timesteps": 400000,
    "seed": 0,
    "n_steps": 50,
    "batch_size": 200,
    "n_epochs": 10,
    "learning_rate": 0.0003,
    "gamma": 0.97,
    "net_arch": [
      64,
      64
    ],
    "eval_episodes": 3,
    "log_std_init": -1.5,
    "early_stop": {
      "metric": "final_abs_err",
      "threshold": 0.03,
      "window": 200,
      "min_timesteps": 50000
    }
  }
}
