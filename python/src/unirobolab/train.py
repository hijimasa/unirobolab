"""Training backend entry point. Placeholder until the first backend lands (M2)."""

from __future__ import annotations


def run(contract_path: str, backend: str) -> int:
    print(f"unirobolab train: backend={backend} contract={contract_path} (not implemented)")
    return 2
